---
"@yielded/auth": minor
---

Add the optional iOS `PasskeyReactNative` adapter with `make()` and `layer` for client-local registration, authentication, and capability checks with typed failures, redacted responses, and content-free infrastructure diagnostics. Document associated-domain setup and the native prompt's interruption limits.
